import React from 'react';
import ReactDOM from 'react-dom/client';
import WedHubHome from '../wedfit-erp.jsx';
import './index.css';

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <WedHubHome />
  </React.StrictMode>
);
